# A24 基于大模型的企业合同智能审核与风险预警系统

> M2.1 实现同步（2026-07-12）：已实现 `GET /api/v1/reviews/{reviewId}`。AI 快照未就绪时返回 `REVIEW_RESULT_NOT_READY`；快照就绪后返回 `reviewId`、`reviewStatus`、`reviewStage`、不可变 `aiResult`、空的 `legalReview`/`riskReview` 和等同 AI 有效内容的 `effectiveResult`。响应不包含 `storagePath`、`filePath` 或内部错误细节。`reviewMode=rulesOnly` 尚无已冻结的 AI 内部契约，当前会安全失败为 `REVIEW_FAILED`，不调用完整审核接口。

| 项目名称 | A24 基于大模型的企业合同智能审核与风险预警系统 |
|---|---|
| 文档名称 | 前后端接口文档 |
| 文档版本 | V0.1 |
| 文档状态 | 开发基线 |
| 创建日期 | 2026-07-11 |
| 主要负责人 | 后端负责人：杨雨翰；前端负责人：王崇哲 |
| 参与人员 | AI算法负责人：刘紫雯 |

## 修订记录

| 版本 | 日期 | 说明 | 修订人 |
|---|---|---|---|
| V0.1 | 2026-07-11 | 定义 `/api/v1` REST 基线 | 杨雨翰 |
| V0.1-r1 | 2026-07-11 | 补齐管理员反馈、运行日志与要素修订接口 | 杨雨翰 |
| V0.1-r2 | 2026-07-11 | 调整人工复核接口与角色权限 | 杨雨翰 |

## 1. 通用规范

基地址：`https://{host}/api/v1`；版本以路径维护。受保护接口头部为 `Authorization: Bearer {accessToken}`，JSON 请求使用 `Content-Type: application/json`。时间为 ISO 8601 UTC，如 `2026-07-11T08:00:00.000Z`；分页参数 `page`（从1开始）和 `pageSize`（默认20、最大100）。上传使用 `multipart/form-data`，字段为 `file` 和可选 `name`。

成功响应统一为：

```json
{"code":"OK","message":"success","data":{},"requestId":"req_01H..."}
```

失败响应统一为：

```json
{"code":"AUTH_TOKEN_INVALID","message":"登录凭证无效或已过期","data":null,"requestId":"req_01H..."}
```

所有字段使用 lowerCamelCase；错误码见《07-错误码说明》。`POST /reviews` 必须额外携带 `Idempotency-Key` 请求头；同一键配合相同 `contractId`、`contractFileId` 与 `reviewMode` 返回同一审核记录，不同载荷返回 `IDEMPOTENCY_CONFLICT`。

## 2. 接口清单

| 名称 | 方法 | 路径 | 权限 | 参数/请求体 | 响应数据 | 可能错误码 |
|---|---|---|---|---|---|---|
| 用户登录 | POST | `/auth/login` | 公开 | `username,password` | `accessToken,tokenType,expiresIn,user` | `PARAM_INVALID`,`AUTH_LOGIN_FAILED` |
| 当前用户 | GET | `/users/me` | 用户 | 无 | `id,username,role,userStatus` | `AUTH_TOKEN_INVALID` |
| 用户列表/详情/更新 | GET/PATCH | `/users[/{userId}]` | 管理员 | 列表分页及`username?`,`role?`,`userStatus?`；更新`username?`,`role?`,`userStatus?` | `items,total` / 用户 | `PERMISSION_DENIED`,`USER_NOT_FOUND`,`USER_USERNAME_EXISTS`,`USER_SELF_UPDATE_FORBIDDEN` |
| 更新用户状态 | PATCH | `/users/{userId}` | 管理员 | `userStatus` | 用户 | `USER_NOT_FOUND` |
| 上传合同 | POST | `/contracts` | 普通用户 | `file,name?` | `contractId,contractFileId,contractStatus` | `FILE_TYPE_UNSUPPORTED`,`FILE_TOO_LARGE` |
| 合同列表 | GET | `/contracts` | 普通用户本人；法务仅`legalReview`待办及本人历史；风控仅`riskReview`待办及本人历史；管理员可全量 | 分页、`contractStatus?`,`contractType?`,`ownerId?`（仅管理员） | `items,total` | `PERMISSION_DENIED` |
| 合同详情 | GET | `/contracts/{contractId}` | 合同所有人、当前阶段审核员、已完成该审核的审核员或管理员 | 无 | 合同、文件、最近审核摘要 | `CONTRACT_NOT_FOUND` |
| 删除合同 | DELETE | `/contracts/{contractId}` | 所有人/管理员 | 无 | `contractId,contractStatus` | `CONTRACT_NOT_FOUND` |
| 发起审核 | POST | `/reviews` | 合同所有人（普通用户） | 请求头`Idempotency-Key`；`contractId,contractFileId,reviewMode` | `reviewId,reviewStatus,reviewStage,requestId` | `CONTRACT_FILE_NOT_FOUND`,`IDEMPOTENCY_CONFLICT`,`REVIEW_ALREADY_RUNNING`,`AI_SERVICE_UNAVAILABLE` |
| 审核进度 | GET | `/reviews/{reviewId}/progress` | 合同所有人、处于当前阶段的审核员/管理员 | 无 | `reviewId,reviewStatus,reviewStage,progress,aiResultAvailable,errorCode?` | `REVIEW_NOT_FOUND` |
| 审核结果 | GET | `/reviews/{reviewId}` | 合同所有人、已完成或处于当前阶段的审核员/管理员 | 无 | `reviewId,reviewStatus,reviewStage,aiResult,legalReview,riskReview,effectiveResult` | `REVIEW_RESULT_NOT_READY` |
| 风险详情/修订 | GET/PATCH | `/risks/{riskId}` | GET：有数据权限用户；PATCH：法务仅`legalReview`、风控仅`riskReview`，且任务未领取或由本人领取 | `riskLevel?`,`suggestion?`,`riskStatus?`,`comment?` | 风险对象及新增修订记录 | `RISK_NOT_FOUND`,`REVIEW_ROLE_NOT_ALLOWED`,`REVIEW_STAGE_INVALID`,`REVIEW_ALREADY_CLAIMED` |
| 修订合同类型 | PATCH | `/reviews/{reviewId}/contract-type` | 法务审核员，仅`legalReview`，且任务未领取或由本人领取 | `contractType`（必填）、`comment?` | 有效合同类型及新增修订记录 | `REVIEW_NOT_FOUND`,`REVIEW_ROLE_NOT_ALLOWED`,`REVIEW_STAGE_INVALID`,`REVIEW_ALREADY_CLAIMED` |
| 修订合同要素 | PATCH | `/reviews/{reviewId}/elements/{elementId}` | 法务审核员，仅`legalReview`，且任务未领取或由本人领取 | `value`（必填字符串）、`comment?` | 有效要素及新增修订记录 | `REVIEW_NOT_FOUND`,`CONTRACT_ELEMENT_NOT_FOUND`,`REVIEW_ROLE_NOT_ALLOWED`,`REVIEW_STAGE_INVALID`,`REVIEW_ALREADY_CLAIMED` |
| 修订总体风险 | PATCH | `/reviews/{reviewId}/overall-risk` | 风控审核员，仅`riskReview`，且任务未领取或由本人领取 | `overallRiskLevel`、`overallScore`、`comment?` | 有效总体风险及新增修订记录 | `REVIEW_NOT_FOUND`,`REVIEW_ROLE_NOT_ALLOWED`,`REVIEW_STAGE_INVALID`,`REVIEW_ALREADY_CLAIMED` |
| 提交反馈 | POST | `/reviews/{reviewId}/feedback` | 法务审核员、风控审核员，仅各自阶段，且任务未领取或由本人领取 | `targetType,targetId?,judgment,correctedValue?,comment?` | `feedbackId` | `FEEDBACK_INVALID`,`REVIEW_ROLE_NOT_ALLOWED`,`REVIEW_STAGE_INVALID`,`REVIEW_ALREADY_CLAIMED` |
| 法务审核确认 | POST | `/reviews/{reviewId}/legal-confirm` | 法务审核员，仅`legalReview`，且任务未领取或由本人领取 | `opinion?` | `reviewId,reviewStatus,reviewStage,legalReview` | `REVIEW_ROLE_NOT_ALLOWED`,`REVIEW_STAGE_INVALID`,`REVIEW_ALREADY_CLAIMED` |
| 风控审核确认 | POST | `/reviews/{reviewId}/risk-confirm` | 风控审核员，仅`riskReview`，且任务未领取或由本人领取 | `opinion?` | `reviewId,reviewStatus,reviewStage,riskReview` | `REVIEW_ROLE_NOT_ALLOWED`,`REVIEW_STAGE_INVALID`,`REVIEW_LEGAL_NOT_COMPLETED`,`REVIEW_ALREADY_CLAIMED` |
| 反馈列表 | GET | `/feedback` | 管理员 | 分页、`reviewId?`,`contractId?`,`submitterId?`,`submitterRole?`,`feedbackType?`,`judgment?`,`from?`,`to?` | `items,total` | `PERMISSION_DENIED` |
| 标准条款管理 | GET/POST/PATCH/DELETE | `/standard-clauses[/{clauseId}]` | 管理员 | `name,contractType,clauseType,content,configStatus?`；DELETE为停用 | 条款对象/分页 | `STANDARD_CLAUSE_NOT_FOUND`,`STANDARD_CLAUSE_EXISTS` |
| 风险规则管理 | GET/POST/PATCH/DELETE | `/risk-rules[/{ruleId}]` | 管理员 | `ruleCode,riskType,name,riskLevel,ruleContent,standardClauseId?,configStatus?`；DELETE为停用 | 规则对象/分页 | `RISK_RULE_NOT_FOUND`,`RISK_RULE_EXISTS` |
| Dashboard统计 | GET | `/dashboard/summary` | 管理员 | `from?`,`to?`，最大366天 | 合同/审核状态、阶段、当前有效风险、上传趋势 | `PERMISSION_DENIED`,`PARAM_INVALID` |
| 历史审核 | GET | `/reviews` | 普通用户本人；审核员查看当前阶段待办及本人复核记录；管理员可全量 | 分页、`contractId?`,`reviewStatus?`,`reviewStage?`,`ownerId?`（仅管理员） | `items,total` | `PERMISSION_DENIED` |
| 运行日志 | GET | `/operation-logs` | 管理员 | 分页、`operatorId?`,`operatorRole?`,`action?`,`targetType?`,`targetId?`,`reviewId?`,`from?`,`to?` | `items,total` | `PERMISSION_DENIED` |
| 报告列表/生成 | GET/POST | `/reviews/{reviewId}/reports` | GET遵循审核可见性；POST仅合同所有人（审核已完成）/管理员 | 分页；POST为`reportFormat` | `items,total` / 报告对象；同一格式重复请求返回既有记录 | `REPORT_FORMAT_UNSUPPORTED`,`REPORT_NOT_READY` |
| 报告详情 | GET | `/reports/{reportId}` | 合同所有人、有权审核员或管理员 | 无 | 报告对象 | `REPORT_NOT_FOUND` |
| 报告重试 | POST | `/reports/{reportId}/retry` | 仅管理员，且仅failed | 无 | 报告对象 | `REPORT_NOT_FOUND`,`REPORT_NOT_READY`,`REPORT_GENERATION_FAILED` |
| 下载报告 | GET | `/reports/{reportId}/download` | 合同所有人、有权审核员或管理员 | 无 | 文件流 | `REPORT_NOT_FOUND`,`REPORT_NOT_READY`,`REPORT_GENERATION_FAILED`,`REPORT_FILE_NOT_FOUND` |

风控确认成功后，后端自动创建默认 `html` 报告记录并交由同一任务执行器生成；`POST /reviews/{reviewId}/reports` 用于按需请求 `html` 或 `pdf`，同一审核与格式重复调用必须返回既有报告记录，不重复生成。

法务和风控首次执行本表中的阶段写操作时，后端必须在同一事务内自动领取对应任务：审核人字段为空则写入当前用户；已为当前用户则继续；已为其他用户则返回 `REVIEW_ALREADY_CLAIMED`。业务写操作失败时，领取必须一并回滚。

## 3. 关键报文示例

登录请求与响应：

```json
{"username":"demo","password":"***"}
```

```json
{"code":"OK","message":"success","data":{"accessToken":"<jwt>","tokenType":"Bearer","expiresIn":3600,"user":{"id":1,"username":"demo","role":"user","userStatus":"active"}},"requestId":"req_x"}
```

发起审核请求：

```json
{"contractId":1001,"contractFileId":2001,"reviewMode":"full"}
```

审核结果响应的 `data` 与《05-AI服务接口文档》的完整结果结构保持一致，并额外包含 `reviewId`、`reviewStatus`、`reviewStage`、`aiResult`（不可变AI初审结果）、`legalReview`、`riskReview` 和 `effectiveResult`（由AI初审与人工修订计算的当前有效结果）。`aiResult` 在 `reviewStage=legalReview` 起可读取，`REVIEW_RESULT_NOT_READY` 仅表示 AI 初审尚不可用。`legalReview` 至少包含 `reviewerId,reviewedAt,opinion,revisions`，`riskReview` 至少包含 `reviewerId,reviewedAt,opinion,revisions,overallRiskLevel,overallScore`。风险对象必须包含 `riskType`、`riskName`、`riskLevel`、`clauseText`、`page`、`paragraphIndex`、`basis`、`suggestion`、`confidence`、`ruleId`（整数，可空）。所有 PATCH 仅追加修订记录，禁止覆盖AI初审行；普通用户对审核结果只有查看权限。

## 4. 前端约定

Axios 响应拦截器统一处理 `code`；401 清理本地登录态并跳转登录页，403 展示无权限。前端不得拼接存储路径、直连 AI 服务或 Qwen API。长任务采用轮询进度接口，

轮询间隔：2秒
AI轮询停止条件：`reviewStage` 不再为 `aiReview`，或 `reviewStatus` 为 `failed`、`cancelled`
最长自动轮询：5分钟
超过5分钟：停止自动轮询，提示用户手动刷新。

## M2.2 实现补充（2026-07-13）

人工复核接口均接入 `/api/v1`。法务仅能写 `processing/legalReview`，风控仅能写 `processing/riskReview`，管理员与普通用户没有人工复核写权限。首次写操作在同一事务自动领取，已由他人领取时返回 `REVIEW_ALREADY_CLAIMED`。

`effectiveResult` 从不可变 `aiResult` 的深拷贝开始，按 `createdAt,id` 升序叠加 `review_revision`；要素和风险分别返回数据库生成的 `elementId`、`riskId`，`dismissed` 风险仍保留。确认迁移为 `processing/legalReview -> processing/riskReview -> completed/completed`，最终确认同时将合同置为 `reviewed` 并创建唯一的 `pending/html` 报告记录，但不生成文件。

## M3.1 管理后台与 Dashboard 实现补充（2026-07-13）

M3.1 使用本表既有非 `/admin` 路径，不创建同义接口。所有接口均需 JWT 且仅 `admin` 可访问；管理员仍不能调用法务/风控修订、反馈或确认接口。用户管理仅实现查询和更新，不提供注册、密码重置或找回密码；管理员不能将自己降级或停用。所有列表使用 `page`、`pageSize`（最大100），并按 `createdAt DESC,id DESC` 稳定排序。

标准条款与风险规则的 DELETE 为 `configStatus=disabled` 停用，不物理删除；创建、修改、停用以及用户更新与操作日志在同一事务提交。配置变更只影响后续审核，不更新历史 `ruleSnapshot`、`aiResult` 或人工修订记录。管理员反馈与操作日志接口只读；操作日志响应统一脱敏密码、令牌、密钥和内部路径字段。

Dashboard 返回 `contractsTotal`、`contractsByStatus`、`reviewsTotal`、`reviewsByStatus`、`reviewsByStage`、`effectiveRisksByLevel`、`pendingLegalReview`、`pendingRiskReview`、`completedReviews` 与 `contractUploadTrend`。合同和关联审核统计排除逻辑删除合同；未提供时间范围时总量为全量、趋势为最近7个UTC自然日，提供单边时间时补齐为不超过366天的窗口。风险分布统计所有已存在AI快照的审核，不限于 completed；批量读取 `risk_record` 与 `review_revision`，按 `created_at ASC,id ASC` 叠加风险修订，以最终 `riskLevel` 计数，最终 `riskStatus=dismissed` 的风险不计入分布。该口径与审核详情的 `effectiveResult` 一致。

M3.2 的 HTML/PDF 文件生成、报告任务、重试和下载仍未实现；M2.2 创建的唯一 `pending/html` 占位记录不会在 M3.1 被消费。

## M3.2 报告生成与下载（2026-07-13）

报告统一基于审核详情的 `effectiveResult` 构建；合同类型、要素、总体风险和风险列表均应用全部人工修订。最终状态为 `dismissed` 的风险不进入当前有效风险列表和等级汇总，只显示已忽略数量。HTML 使用自动转义模板和内嵌样式，PDF 使用同一结构化上下文直接排版。

报告状态为 `pending -> generating -> completed`，失败转为 `failed`。数据库执行器原子领取 pending 或超过失活阈值的 generating 任务，最多尝试 `REPORT_MAX_ATTEMPTS` 次。文件在受控目录临时写入、原子替换成功后才提交 completed；数据库完成更新失败时删除已发布文件。retry 只允许管理员将未超过次数上限的 failed 记录恢复为 pending。

下载前同时校验审核数据权限、completed 状态、相对路径边界、普通文件存在性和文件大小。HTML 使用 `text/html; charset=utf-8`，PDF 使用 `application/pdf`；响应不包含服务器绝对路径。

## 交付前 OpenAPI 一致性补充（2026-07-13）

FastAPI OpenAPI 中所有外部 path 参数统一使用 lowerCamelCase（如 `contractId`、`reviewId`、`elementId`、`riskId`、`reportId`），与本文件一致；内部 Python 参数继续使用 snake_case。除 `/health` 与 `/api/v1/auth/login` 外，所有业务接口均声明 Bearer 鉴权。`reviewMode`、`reportFormat`、角色、合同类型、风险类型、风险等级和配置状态均在请求 Schema 中暴露枚举约束，不保留同义字段或同义接口。

OpenAPI、Swagger UI 和 ReDoc 可通过 `OPENAPI_ENABLED` 控制；生产环境默认关闭。运行时成功/失败响应仍严格使用本文件第1节统一包装，文件下载接口按实际文件格式返回流式响应。
