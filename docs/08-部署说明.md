# A24 基于大模型的企业合同智能审核与风险预警系统

> M2.1 实现同步（2026-07-12）：新增环境变量 `TASK_EXECUTOR_ENABLED`、`TASK_POLL_SECONDS`、`TASK_STALE_SECONDS`、`AI_RETRY_COUNT`、`AI_RETRY_BASE_SECONDS`。AI 执行器内置于业务后端，生产环境必须使用 `uvicorn app.main:app --workers 1`；测试环境应设置 `TASK_EXECUTOR_ENABLED=false`。AI 服务健康检查仍访问服务根路径 `/health`，完整审核调用为 `{AI_SERVICE_BASE_URL}/reviews/full`。

| 项目名称 | A24 基于大模型的企业合同智能审核与风险预警系统 |
|---|---|
| 文档名称 | 部署说明 |
| 文档版本 | V0.1 |
| 文档状态 | 开发基线 |
| 创建日期 | 2026-07-11 |
| 主要负责人 | 后端负责人：杨雨翰 |
| 参与人员 | AI算法负责人：刘紫雯；前端负责人：王崇哲 |

## 修订记录

| 版本 | 日期 | 说明 | 修订人 |
|---|---|---|---|
| V0.1 | 2026-07-11 | 建立开发与Docker部署框架 | 杨雨翰 |
| V0.1-r1 | 2026-07-11 | 补充四角色演示账号初始化要求 | 杨雨翰 |

## 1. 环境要求

开发环境：Python 3.11+、Node.js 20 LTS、MySQL 8.0+、Docker Engine 24+、Docker Compose v2、Git。Qwen API 需要有效的企业/个人授权，具体模型为qwen3.7-plus，配额为 TBD。赛题不强制实体硬件；演示环境可采用CPU解析和云端模型API，实际资源以压测结果为准。

## 2. 服务、端口与存储

| 服务 | 容器内端口 | 对外暴露 | 说明 |
|---|---:|---:|---|
| nginx | 80/443 | 80/443 | 静态前端、反向代理、TLS终止 |
| frontend | 构建为静态文件 | 否 | 由Nginx提供 |
| backend | 8000 | 否（开发可8000） | 业务REST API |
| ai-service | 8001 | 否 | 仅供backend内部调用 |
| mysql | 3306 | 否（开发可3306） | 业务数据 |

持久化目录规划：`storage/uploads/` 保存原始上传文件，`storage/reports/` 保存HTML/PDF报告，`logs/` 保存应用日志。生产环境上述目录以 Docker volume 挂载并设置最小访问权限；严禁将其提交到Git。

## 3. 环境变量

以下仅为变量名与占位符，不得提交真实密码或API Key：

```dotenv
# frontend
VITE_API_BASE_URL=https://example.com/api/v1
# backend
APP_ENV=development
DATABASE_URL=mysql+pymysql://<DB_USER>:<DB_PASSWORD>@mysql:3306/a24_contract_audit
JWT_SECRET=<JWT_SECRET>
JWT_EXPIRE_SECONDS=3600
AI_SERVICE_BASE_URL=http://ai-service:8001/internal/v1
AI_INTERNAL_TOKEN=<AI_INTERNAL_TOKEN>
UPLOAD_ROOT=/data/uploads
REPORT_ROOT=/data/reports
REPORT_EXECUTOR_ENABLED=true
REPORT_TASK_POLL_SECONDS=1
REPORT_TASK_STALE_SECONDS=300
REPORT_MAX_ATTEMPTS=3
MAX_UPLOAD_MB=20
# ai-service
QWEN_API_KEY=<QWEN_API_KEY>
QWEN_MODEL=qwen3.7-plus
PROMPT_VERSION=v0.1
INTERNAL_TOKEN=<AI_INTERNAL_TOKEN>
```

`AI_INTERNAL_TOKEN` 与 `INTERNAL_TOKEN` 必须一致且仅存在于部署密钥管理/环境变量中；`QWEN_API_KEY` 不可传递给前端、日志或数据库。

## 4. 本地启动与数据库迁移

推荐顺序：启动 MySQL；配置 backend 与 ai-service 环境变量；执行 Alembic 迁移；启动 AI 服务；启动业务后端；最后启动前端。数据库初始化通过 `alembic upgrade head` 执行，任何结构变更先创建审核过的迁移，再在目标环境执行。初始化演示数据时分别创建普通用户、法务审核员、风控审核员和管理员账号；不同角色使用不同账号，不得用同一普通用户账号完成合同发起和人工审核。禁止手工修改生产表结构，且不得写入真实账号、密码或密钥。

## 5. Docker Compose规划

`docker-compose.yml` 规划 `mysql`、`backend`、`ai-service`、`nginx` 四个服务，前端由构建阶段产物交给 Nginx。backend 与 ai-service 共享只读/受控的上传卷，报告卷仅由 backend 写入；MySQL 不对公网暴露。服务健康检查分别使用 `/health`，MySQL使用原生健康检查；Nginx 仅在依赖健康后接流量。

## 6. 运维检查与常见问题

| 现象 | 检查与处理 |
|---|---|
| 上传后审核失败 | 检查文件格式、共享卷挂载、AI服务健康状态和任务错误码 |
| OCR失败 | 检查图片清晰度、PaddleOCR依赖与语言配置 |
| 大模型超时/限流 | 检查 Qwen 配额、网络和重试日志；不要泄露Key |
| 前端401/403 | 检查JWT过期、反向代理头和用户角色 |
| 数据库迁移失败 | 停止发布，核对当前Alembic版本和备份策略后处理 |

监控指标、生产备份周期、TLS证书来源为 TBD，须在部署验收前确认。上传大小：20MB，超时阈值：默认连接超时 5 秒、读取超时 120 秒、最多重试：2次。

## M2.2 部署说明（2026-07-13）

M2.2 未新增环境变量和 Alembic 版本，数据库 head 仍为 `20260712_0003`。自动领取依赖 MySQL 8 InnoDB 行锁，正式数据库不得替换为 SQLite。风控确认只写入 `pending/html` 报告记录；当前版本没有报告文件生成器。

## M3.2 部署说明（2026-07-13）

业务后端新增 Jinja2 与 ReportLab 依赖。HTML 自包含且不加载远程资源；PDF 使用随应用打包并嵌入PDF子集的 `NotoSansSC-VF.ttf`，字体遵循 SIL OFL 1.1，不依赖开发机或容器系统字体。`REPORT_ROOT` 必须挂载为仅 backend 可写的持久卷，不能映射到公开静态目录。

报告执行器内置于 backend，并通过 `REPORT_EXECUTOR_ENABLED` 单独启停。与AI执行器相同，当前部署应保持单 Uvicorn worker；多实例部署时由数据库条件更新防止重复领取。测试环境默认关闭执行器并显式调用 `ReportExecutor.run_once()`。

## 后端 Docker 交付补充（2026-07-13）

`backend/Dockerfile` 使用固定 Python 3.11 slim 基础镜像，从 `pyproject.toml` 安装依赖，并打包 Alembic、HTML模板、Noto字体及OFL许可证。容器以非root用户运行，监听8000端口，内置只访问 `GET /health` 的轻量健康检查。`.dockerignore` 排除 `.env`、密钥、测试缓存、上传、报告和日志。

容器内默认 `APP_ENV=production`、`OPENAPI_ENABLED=false`、`UPLOAD_ROOT=/data/uploads`、`REPORT_ROOT=/data/reports`。生产启动必须显式提供非默认 `DATABASE_URL`、`JWT_SECRET`、`AI_INTERNAL_TOKEN` 和实际域名 `TRUSTED_HOSTS`，否则后端拒绝启动。迁移使用同一镜像执行 `alembic upgrade head`；上传卷和报告卷必须持久化，多个实例必须共享同一报告卷。

当前仍建议 `uvicorn --workers 1`。如未来启用多个worker，每个worker都会启动lifespan执行器；数据库原子领取防止同一任务重复执行，但所有worker仍必须共享上传与报告存储。存活检查不依赖AI服务，短暂AI故障不会使API进程被判定死亡。
