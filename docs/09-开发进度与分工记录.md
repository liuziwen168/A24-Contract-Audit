# A24 基于大模型的企业合同智能审核与风险预警系统

> M2.1 实现同步（2026-07-12）：已完成 AI 审核编排、受控重试、任务失活恢复、AI 初审快照/要素/风险原子持久化、结果读取接口及 HTTP Mock 自动化测试。未实现法务/风控人工修订、反馈、确认、报告或 Dashboard。

| 项目名称 | A24 基于大模型的企业合同智能审核与风险预警系统 |
|---|---|
| 文档名称 | 开发进度与分工记录 |
| 文档版本 | V0.1 |
| 文档状态 | 开发基线 |
| 创建日期 | 2026-07-11 |
| 主要负责人 | 项目组（王崇哲、刘紫雯、杨雨翰） |
| 参与人员 | 前端：王崇哲；AI算法：刘紫雯；后端：杨雨翰 |

## 修订记录

| 版本 | 日期 | 说明 | 修订人 |
|---|---|---|---|
| V0.1 | 2026-07-11 | 建立三人团队记录模板 | 杨雨翰 |
| V0.1-r1 | 2026-07-11 | 补充四角色人工复核开发任务 | 杨雨翰 |
| V0.1-r2 | 2026-07-11 | 确立评测数据与标注验收基线 | 刘紫雯 |
| V0.1-r3 | 2026-07-12 | 对齐审核状态机、contractFileId、AI初审快照、自动领取与人工复核留痕契约 | 杨雨翰 |

## 1. 团队分工

| 角色 | 责任范围 | 交付物/接口 |
|---|---|---|
| AI算法负责人（刘紫雯） | 文档解析、PaddleOCR、Qwen API、Prompt、分类、要素、风险、条款比对、评分、AI接口、模型评测；区分AI初审结果与人工审核结果，为人工复核提供页码、段落、置信度和依据，收集法务和风控标注用于后续评测 | AI服务、Prompt版本、规则/评测记录 |
| Python后端负责人（杨雨翰） | FastAPI、JWT、四角色RBAC、上传/文件、MySQL、SQLAlchemy、Alembic、AI调用、条款/规则、结果保存、反馈、报告、REST API；法务/风控审核确认接口、修改和标注权限校验、审核人字段和操作日志 | 业务后端、迁移、OpenAPI、部署配置 |
| Vue3前端负责人（王崇哲） | 登录、Dashboard、上传、进度、结果/原文定位、风险详情、反馈、条款/规则管理、历史/下载、ECharts、Element Plus；法务审核页面、风控审核页面、不同角色菜单和按钮权限、普通用户只读审核结果 | 前端页面、接口适配、联调记录 |

## 2. 里程碑检查表

| 里程碑 | 验收内容 | 负责人 | 计划日期 | 状态 |
|---|---|---|---|---|
| M0 设计冻结 | 九份V0.1文档交叉检查、状态机/留痕/幂等契约确认 | 全员 | TBD | 已建立基线 |
| M1 基础链路 | 登录、上传、数据库迁移、AI健康检查 | 后端/前端/AI | TBD | 未开始 |
| M2 审核闭环 | 完整审核、结果保存、风险展示、反馈 | 全员 | TBD | 未开始 |
| M3 管理与报告 | 条款/规则管理、Dashboard、HTML/PDF导出 | 全员 | TBD | 未开始 |
| M4 评测与演示 | 测试集评测、问题修复、演示材料 | 全员 | TBD | 未开始 |

## 3. 每日开发记录模板

| 日期 | 成员 | 完成事项 | 代码/文档链接 | 阻塞问题 | 次日计划 | 关联提交 |
|---|---|---|---|---|---|---|
| YYYY-MM-DD | 姓名 |  |  |  |  |  |

## 4. 每周计划模板

| 周期 | 本周目标 | 任务与负责人 | 依赖 | 验收标准 | 风险/调整 |
|---|---|---|---|---|---|
| YYYY-Www |  |  |  |  |  |

## 5. 变更与联调记录模板

### 接口变更记录

| 日期 | 接口/字段 | 变更前 | 变更后 | 影响方 | 负责人 | 兼容方案 |
|---|---|---|---|---|---|---|
| YYYY-MM-DD |  |  |  |  |  |  |
| 2026-07-14 | `POST /users` | 前端已有管理员创建用户入口，后端无对应接口 | 新增管理员专用创建接口；创建后默认启用，密码哈希存储，并写入管理员操作日志 | 前端用户管理、后端用户管理、接口文档 | 后端/前端 | 不改变既有 GET/PATCH `/users`；不提供公开注册、密码重置或找回密码 |

### 数据库变更记录

| 日期 | Alembic版本 | 表/字段 | 变更原因 | 回滚方案 | 负责人 | 状态 |
|---|---|---|---|---|---|---|
| YYYY-MM-DD |  |  |  |  |  |  |

### Prompt版本记录

| 日期 | promptVersion | 场景 | 变更摘要 | 评测集/指标 | 负责人 | 回退版本 |
|---|---|---|---|---|---|---|
| YYYY-MM-DD | v0.1 |  |  | TBD |  |  |

### 联调与测试记录

| 日期 | 场景 | 请求/数据版本 | 预期 | 实际 | 缺陷/错误码 | 负责人 | 结论 |
|---|---|---|---|---|---|---|---|
| YYYY-MM-DD |  |  |  |  |  |  |  |

### 问题与解决记录

| 编号 | 日期 | 问题 | 影响 | 处理方案 | 负责人 | 状态 |
|---|---|---|---|---|---|---|
| ISSUE-001 | YYYY-MM-DD |  |  |  |  | open |

## 6. Git提交规范

提交信息格式：`<type>(<scope>): <summary>`。`type` 限 `feat`、`fix`、`docs`、`refactor`、`test`、`chore`；`scope` 示例为 `frontend`、`backend`、`ai`、`docs`。一次提交只解决一个可说明的变更；接口、迁移或Prompt变更必须同步填写对应记录，禁止提交真实密钥、合同样本原件或生成的报告文件。

## 7. 当前TBD清单

| 项目 | 需确认内容 | 建议负责人 |
|---|---|---|
| 成员姓名与计划日期 | 三名成员实名、可投入时间与里程碑日期 | 全员 |
| 测试集与标注规范 | 按《05-AI服务接口文档》建立不少于100份脱敏样本、80/20切分、双人标注与冻结测试集评测记录 | 刘紫雯 |
| 上传/超时/轮询阈值 | 文件大小、AI超时、前端轮询参数 | 杨雨翰、王崇哲 |
| 部署与TLS | 目标主机、域名、证书、备份策略 | 杨雨翰 |

## M2.2 完成记录（2026-07-13）

已完成法务复核、风控复核、反馈、不可变人工修订、有效结果计算、自动领取、阶段确认、默认报告占位和四角色数据范围。现有表结构满足需求，因此未创建空的 `0004` 迁移，head 保持 `20260712_0003`。真实 MySQL 已完成 upgrade/downgrade/upgrade，并分别验证法务与风控并发领取仅一人成功。M3 的报告文件生成/下载、Dashboard、用户与规则管理仍未实现。

## M3.1 完成记录（2026-07-13）

已完成仅管理员可用的用户创建/查询/更新、标准条款管理、风险规则管理、反馈只读列表、操作日志只读查询和 Dashboard 统计。管理员写操作与结构化操作日志同事务提交；配置删除采用停用，不影响历史AI快照、规则快照和人工修订。Dashboard 排除逻辑删除合同，按 `created_at,id` 叠加风险修订并排除最终 `dismissed` 风险，覆盖法务、风控及已完成阶段的当前有效风险。现有12张表完整支持本阶段，因此未新增迁移，head 仍为 `20260712_0003`。

M3.2 报告文件生成、报告任务、重试和下载仍未实现，既有 `pending/html` 报告占位记录保持不变。

## M3.2 完成记录（2026-07-13）

已实现基于 `effectiveResult` 的 HTML/PDF 报告、数据库任务领取与失活恢复、失败和管理员重试、幂等创建、列表/详情/下载权限、受控相对路径和原子文件写入。最终 `dismissed` 风险不进入有效列表和汇总。新增 `20260713_0004_report_executor`，只扩展既有 `report` 表的任务与文件元数据；未修改历史迁移。真实 MySQL 使用独立 `a24_m32_audit_20260713` 验收库。

## 后端交付前总验收记录（2026-07-13）

完成普通用户上传与幂等发起、真实httpx受控AI响应、AI快照持久化、法务/风控自动领取与修订、反馈、两阶段确认、HTML/PDF生成、下载权限、Dashboard、反馈和日志查询的完整端到端测试。新增最终真实MySQL白名单库 `a24_backend_final_audit_20260713`，迁移执行 `upgrade head -> downgrade base -> upgrade head` 后保持 `20260713_0004`。

OpenAPI path参数已与lowerCamelCase契约对齐，请求Schema补齐关键枚举；新增生产配置fail-fast、Trusted Host与OpenAPI开关。后端新增最小Dockerfile和.dockerignore，未修改frontend、ai-service或0001至0004历史迁移。Docker构建命令已执行，但当时主机Docker Desktop Linux engine未运行，镜像启动、卷持久化和容器内报告验证仍需在Docker daemon可用环境补验。
