# A24 基于大模型的企业合同智能审核与风险预警系统

| 项目名称 | A24 基于大模型的企业合同智能审核与风险预警系统 |
|---|---|
| 文档名称 | 数据字典 |
| 文档版本 | V0.1 |
| 文档状态 | 开发基线 |
| 创建日期 | 2026-07-11 |
| 主要负责人 | 后端负责人：杨雨翰 |
| 参与人员 | AI算法负责人：刘紫雯；前端负责人：王崇哲 |

## 修订记录

| 版本 | 日期 | 说明 | 修订人 |
|---|---|---|---|
| V0.1 | 2026-07-11 | 建立跨服务枚举与命名基线 | 杨雨翰 |
| V0.1-r1 | 2026-07-11 | 补齐风险、报告、配置与要素来源状态 | 杨雨翰 |
| V0.1-r2 | 2026-07-11 | 补充审核角色与审核阶段枚举 | 杨雨翰 |

## 1. 命名与格式规范

外部 JSON 全部为 lowerCamelCase；Python 内部可使用 snake_case，并通过 Pydantic alias 转换。数据库表/列为 snake_case。日期使用 `YYYY-MM-DD`，时间使用 ISO 8601 UTC（`YYYY-MM-DDTHH:mm:ss.SSSZ`）；金额使用十进制字符串或 `decimal(18,2)`，单位人民币元，禁止浮点金额。置信度范围 `[0,1]`，风险总分范围 `[0,100]`。

## 2. 业务枚举

| 字典 | 值 | 中文含义 |
|---|---|---|
| 合同类型 `contractType` | `purchase`,`sales`,`nda`,`outsourcing`,`labor`,`other` | 采购、销售、保密、服务外包、劳动、其他 |
| 风险等级 `riskLevel` | `high`,`medium`,`low` | 高、中、低 |
| 审核状态 `reviewStatus` | `pending`,`processing`,`completed`,`failed`,`cancelled` | 待处理、处理中、成功、失败、已取消 |
| 合同状态 `contractStatus` | `uploaded`,`reviewing`,`reviewed`,`failed`,`deleted` | 已上传、审核中、已审核、审核失败、已删除 |
| 文件类型 `fileType` | `docx`,`pdf`,`image` | Word、PDF、图片 |
| 用户角色 `role` | `user`,`legalReviewer`,`riskReviewer`,`admin` | 普通用户/合同经办人员、法务审核员、风控审核员、管理员 |
| 审核阶段 `reviewStage` | `aiReview`,`legalReview`,`riskReview`,`completed` | AI初审、法务复核、风控复核、已完成 |
| 用户状态 `userStatus` | `active`,`disabled` | 正常、停用 |
| 反馈判断 `judgment` | `correct`,`incorrect`,`modified` | 正确、错误、已修订 |
| 报告格式 `reportFormat` | `html`,`pdf` | HTML、PDF |
| 审核模式 `reviewMode` | `full`,`rulesOnly` | 完整审核、仅规则审核 |
| 风险记录状态 `riskStatus` | `active`,`modified`,`dismissed` | 有效、已修订、已忽略 |
| 报告状态 `reportStatus` | `pending`,`generating`,`completed`,`failed` | 待生成、生成中、已完成、失败 |
| 配置状态 `configStatus` | `active`,`disabled` | 启用、停用；用于标准条款和风险规则 |
| 要素来源 `elementSource` | `ai`,`user` | AI抽取、人工新增；对既有AI要素的修订写入`review_revision`，不覆盖其来源 |
| 修订目标 `revisionTargetType` | `contractType`,`element`,`risk`,`overallRisk` | 合同类型、要素、单项风险、总体风险 |
| 反馈目标 `feedbackTargetType` | `contractType`,`element`,`risk`,`overallRisk` | 反馈标注所指向的审核结果 |

### 2.1 审核状态机

`reviewStatus` 表示整份合同审核的生命周期，`reviewStage` 表示当前可执行的业务阶段；二者必须同时返回、同时持久化，且不得用 `reviewStatus=completed` 表示“仅 AI 初审完成”。状态组合仅允许如下取值：

| `reviewStatus` | `reviewStage` | 含义与可执行操作 |
|---|---|---|
| `pending` | `aiReview` | 已创建、待任务执行器领取 |
| `processing` | `aiReview` | AI 正在解析或审核 |
| `processing` | `legalReview` | AI 初审已保存，法务审核员可复核；AI结果可读取 |
| `processing` | `riskReview` | 法务已确认，风控审核员可复核；AI与法务结果可读取 |
| `completed` | `completed` | 风控已确认，最终结果与报告可读取 |
| `failed` | `aiReview` | AI 初审失败，可由合同所有人重新发起新的审核 |
| `cancelled` | `aiReview` | AI 初审开始前已取消 |

状态迁移固定为：`pending/aiReview → processing/aiReview → processing/legalReview → processing/riskReview → completed/completed`；AI失败转为 `failed/aiReview`，仅 AI 初审开始前允许转为 `cancelled/aiReview`。`contractStatus` 随之为 `uploaded → reviewing → reviewed`；AI失败时为 `failed`。重新上传或重新审核必须创建新的文件版本和新的审核记录。

## 3. 风险类型（不少于10类）

| `riskType` | 风险名称 | 默认等级建议 |
|---|---|---|
| `unlimitedLiability` | 无限责任 | high |
| `excessiveLiquidatedDamages` | 违约金过高 | high |
| `unilateralTermination` | 单方解除权 | high |
| `unfairPaymentTerms` | 不合理付款条件 | medium |
| `unfavorableJurisdiction` | 管辖/争议解决不利 | medium |
| `missingDisputeResolution` | 缺失争议解决条款 | medium |
| `overbroadConfidentiality` | 保密义务过宽 | medium |
| `missingConfidentiality` | 缺失保密条款 | medium |
| `missingPerformanceTerm` | 缺失履行期限 | medium |
| `ambiguousAcceptance` | 验收标准不明确 | medium |
| `intellectualPropertyUnclear` | 知识产权归属不清 | medium |
| `forceMajeureMissing` | 缺失不可抗力条款 | low |

默认等级仅为规则初值；审核输出等级以规则和人工复核为准。

## 4. 要素与核心字段

| `elementType` | `elementName` | 值格式 |
|---|---|---|
| `partyA` | 合同甲方 | 文本 |
| `partyB` | 合同乙方 | 文本 |
| `signingDate` | 签署日期 | `YYYY-MM-DD` 或原文文本 |
| `contractAmount` | 合同金额 | 数字字符串，元 |
| `performanceTerm` | 履行期限 | 文本或标准日期区间 |
| `disputeResolution` | 争议解决方式 | 文本 |

关键外部字段：`requestId`（字符串，调用追踪）、`contractId`/`contractFileId`/`reviewId`/`riskId`/`ruleId`（整数ID，`ruleId`可空）、`typeConfidence`/`confidence`（0至1）、`page`（从1开始的整数，可空）、`paragraphIndex`（从0开始的整数，可空）、`processingTimeMs`（非负整数）、`missingClauses`（条款类型字符串数组）、`warnings`（字符串数组）、`error`（对象或 null）。审核资源必须返回 `reviewStatus` 与 `reviewStage`；`reviewStatus=processing` 且 `reviewStage` 为 `legalReview` 或 `riskReview` 时，`aiResult` 已可读取。数据库 `status` 字段在外部 JSON 中按资源类型映射为 `userStatus`、`contractStatus`、`reviewStatus`、`riskStatus`、`reportStatus` 或 `configStatus`，不得使用无上下文的 `status` 作为业务契约字段。

## 5. 变更控制

新增枚举不得复用既有值；废弃值先在字典标记并保留兼容读取。字典变更必须同时更新数据库校验、Pydantic schema、前端显示映射、接口文档和错误码说明，并记录在《09-开发进度与分工记录》。
